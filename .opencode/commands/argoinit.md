---
description: Initialize the Argo codebase for development.
agent: Init
---

Call the unified `argo` MCP tool `initializeWorkspace` to set up the Argo codebase for development. This prepares the workspace bootstrap assets and resets stage handoff artifacts.